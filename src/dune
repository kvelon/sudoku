(library
  (name sudoku)
  (inline_tests)                
  (preprocess (pps ppx_inline_test ppx_expect))
  (wrapped false)
  (libraries core batteries graphics ysc2229)
  (synopsis "Solving sudoku"))

(env
 (release (inline_tests enabled)))
